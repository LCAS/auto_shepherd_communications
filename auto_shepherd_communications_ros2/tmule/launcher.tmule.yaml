session: communicator
init_cmd: |
  echo " "
  echo " "
  echo " "
  echo "Running Initialisation"

  #exec /bin/bash
  cd @TMULE_CONFIG_DIR@ || true
  set -o pipefail


windows:

#  - name: data_loader
#    panes:
#      - ros2 run auto_shepherd_communications_ros2 data_loader_node.py

  - name: communicator
    panes:
      - ros2 run auto_shepherd_communications_ros2 serial_node.py
